bot:
  max_prs_per_day: 5
  max_diff_lines: 200

  min_confidence_score: 0.2   # 🔥 lower more

  search_labels:
    - "good first issue"

  search_language: ""   # 🔥 remove restriction

  max_issues_per_run: 30

  min_confidence_score: 0.0   # 🔥 allow all

search_language: ""         # 🔥 remove restriction

max_repo_inactivity_days: 9999
min_repo_stars: 0
max_repo_stars: 999999            # 🔥 allow all

  repo_allowlist: []   # 🔥 keep empty
