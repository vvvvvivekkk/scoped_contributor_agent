bot:
  max_prs_per_day: 5
  max_diff_lines: 200

  min_confidence_score: 0.2   # 🔥 lower more

  search_labels:
    - "good first issue"

  search_language: ""   # 🔥 remove restriction

  max_issues_per_run: 30

  min_confidence_score: 0.0   # 🔥 allow all

search_language: ""         # 🔥 remove restriction
bot:
  max_prs_per_day: 2
  max_issues_per_run: 3
  min_confidence_score: 0.2
search_labels:
  - "good first issue"

max_issues_per_run: 5
min_confidence_score: 0.2

  repo_allowlist: []
