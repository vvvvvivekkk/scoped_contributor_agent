bot:
  max_prs_per_day: 5
  max_diff_lines: 200

  min_confidence_score: 0.2   # 🔥 lower more

  search_labels:
    - "good first issue"

  search_language: ""   # 🔥 remove restriction

  max_issues_per_run: 30

  max_repo_inactivity_days: 3650   # 🔥 allow old repos
  max_repo_stars: 100000           # 🔥 allow big repos
  min_repo_stars: 0                # 🔥 allow all

  repo_allowlist: []   # 🔥 keep empty
