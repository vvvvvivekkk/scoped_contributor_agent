bot:
  max_prs_per_day: 5
  max_diff_lines: 200

  min_confidence_score: 0.2   # 🔥 lower more

  search_labels:
    - "good first issue"

  search_language: ""   # 🔥 remove restriction

  max_issues_per_run: 30

  min_confidence_score: 0.0   # 🔥 allow all

search_language: ""         # 🔥 remove restriction
bot:
  max_prs_per_day: 2
  max_issues_per_run: 3
  min_confidence_score: 0.2

  search_labels:
    - "good first issue"
    - "easy"
    - "beginner"

  max_repo_stars: 2000
  min_repo_stars: 0
  max_repo_inactivity_days: 180

  repo_allowlist: []
